{
  "name": "demo_sql_inject",
  "version": "1.0.0",
  "description": "Simple, isolated demo of SQL injection using sqlite with nodejs.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sqlite3": "^5.1.6"
  }
}
